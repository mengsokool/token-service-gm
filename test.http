# ทดสอบระบบจัดเก็บ IoT Device Token

// โย่ววว! ก่อนจะลุยกันต่อ มาทำความเข้าใจกันหน่อย!
// token ของเราต้องเป็น string เท่านั้นนะจ๊ะ! ห้ามใช้ number หรืออะไรแปลกๆ เด็ดขาด!
// ไม่งั้นระบบจะงงเหมือนเวลาครูถามแล้วเราตอบไม่ได้เลย 555

###
// โอเค! ทดสอบแรกเลย! เราจะลองแอบเข้าบ้านคนอื่นดู (เอ๊ย! ลองเช็ค token ที่ยังไม่เคยลงทะเบียน)
// ถ้ายังไม่เคยลงทะเบียน ระบบจะตอบกลับมาว่า {"allow": false}
// แปลว่า "เฮ้ยย! นายเป็นใครวะ? ไม่เคยเห็นหน้า ไม่ให้เข้านะโว้ยยย!"
GET http://localhost:6000/verify/fucking_secret_tokennn555

// เอ๊ะ! ทำไมต้องใช้ GET ล่ะ? 
// ก็เพราะว่าเราแค่อยากดูข้อมูลเฉยๆ ไม่ได้จะแก้ไขอะไร เหมือนตอนที่แอบส่องโปรไฟล์แฟนเก่าใน IG ไง! 😂
// แล้วทำไมต้องส่ง token ผ่าน URL ล่ะ? 
// เพราะมันเร็วและง่ายสุดแล้วอ่ะ! เหมือนตอนที่เราตะโกนเรียกเพื่อนข้ามถนน แทนที่จะเดินไปกระซิบข้างหู 🤣

// รู้ยัง? ถ้าเราใช้ POST แทน GET ในการเช็ค token ก็ได้นะ แต่มันจะดูเหมือนเราพยายามยัดเยียดตัวเองเข้าไปในปาร์ตี้ที่เขาไม่ได้เชิญ! 😅

###
// เอาล่ะ! ถ้าอยากเข้าบ้านได้ ก็ต้องลงทะเบียนก่อนสิวะ! (ลงทะเบียน token ใหม่)
// ส่ง POST request ไปที่ /register พร้อมกับ token สุดเจ๋งของเรา
// หลังจากนี้ ระบบจะจำหน้าเราได้แล้ว! (เก็บ token เข้าระบบ)
POST http://localhost:6000/register
Content-Type: application/json

{
    "token": "fucking_secret_tokennn555"
}

// โอ้โห! ทำไมต้องใช้ POST ด้วยล่ะ?
// ก็เพราะเรากำลังจะสร้างข้อมูลใหม่ไง! เหมือนตอนที่เราโพสต์รูปใหม่ลง Facebook ยังไงล่ะ 📸
// แล้วทำไมต้องระบุ Content-Type ด้วยล่ะ?
// เพราะเราต้องบอกเซิร์ฟเวอร์ว่าเรากำลังส่งข้อมูลแบบไหนไปให้ เหมือนตอนที่เราบอกแม่ว่าจะกินข้าวหรือกินก๋วยเตี๋ยว แม่จะได้เตรียมช้อนส้อมหรือตะเกียบให้ถูก! 🍽️

// เกร็ดความรู้: รู้มั้ยว่า POST ไม่ได้จำกัดแค่ส่ง JSON นะ! เราส่งรูป ไฟล์ หรือแม้แต่วิดีโอก็ได้! 
// แต่ถ้าทำแบบนั้น อย่าลืมเปลี่ยน Content-Type ให้ถูกล่ะ ไม่งั้นจะเหมือนใส่รองเท้าส้นสูงไปเตะฟุตบอลเลย 👠⚽

###
// แต่ถ้าวันไหนเบื่อ อยากออกจากแก๊งค์ (ลบ token ออกจากระบบ)
// ก็แค่ส่ง DELETE request มา แล้วบอกลาเพื่อนๆไป~
// ระบบจะลบข้อมูลเราออก เหมือนกับตอนที่เราลบเพื่อนใน Facebook ยังไงยังงั้นเลย!
DELETE http://localhost:6000/invoke/fucking_secret_tokennn555

// เฮ้! ทำไมต้องใช้ DELETE ล่ะ? ทำไมไม่ใช้ POST แล้วส่ง { "action": "delete" } ไปล่ะ?
// ก็เพราะ HTTP มี method ให้เราใช้อยู่แล้วไง! ใช้ให้ถูกกับงานสิ เหมือนตอนที่เราใช้ไม้เบสบอลตีลูกเบสบอล ไม่ใช่เอาไปตีแมลงวัน! 🦟🏏

// แล้วทำไมต้องส่ง token ผ่าน URL อีกล่ะ?
// เพราะมันง่ายและตรงไปตรงมา! เหมือนตอนที่เราชี้นิ้วบอกว่า "เอาอันนี้!" แทนที่จะอธิบายยืดยาวว่าเราต้องการอะไร 👉

// เกร็ดความรู้: จริงๆแล้ว DELETE สามารถมี body ได้นะ! แต่หลายๆ framework ไม่รองรับ 
// เหมือนกับที่เราสามารถใส่ถุงเท้าทับรองเท้าได้ แต่ใครจะทำล่ะ? 🧦👟